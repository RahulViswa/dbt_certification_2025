version: 2

models:
  - name: stg_stripe__payments
    description: The grain of this table is one unique payment per row.
    columns:
      - name: payment_id
        description: The primary key for the payments table.
        data_tests:
          - unique
          - not_null